/** <summary>
 {{ description }}
 </summary> **/
[Name("{{ name }}")]
[Description(@"{{ description }}")]
public abstract class {{ toResolver name }} 
{
    {{#each fields}}

        {{#if hasArguments }}
            /** <summary>
             {{ description }}
            </summary> **/
            [Name("{{ name }}")]
            [Description(@"{{ description }}")]
            public abstract Task<{{ convertedType this }}> {{toPascalCase name }}Async(
            {{#each arguments}}
                [Name("{{ name }}")]
                [Description(@"{{ description }}")]
                {{ convertedType this }} {{ toCamelCase name }}{{ getOptionals this }}{{#unless @last}}, {{/unless}} 
            {{/each}}
            );
        {{else}}
            /** <summary>
             {{ description }}
            </summary> **/
            [Name("{{ name }}")]
            [Description(@"{{ description }}")]
            public abstract Task<{{ convertedType this }}> {{toPascalCase name }}Async(); 
        {{/if}}
    {{/each}} 
}