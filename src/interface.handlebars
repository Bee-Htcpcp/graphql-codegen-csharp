{{ toComment description }}
public interface I{{ name }}{{#if hasInterfaces}} : {{#each interfaces}}{{this}}{{#unless @last}},{{/unless}}{{/each}}{{/if}} {
{{#each fields}}
  /// <summary>
  /// {{ description }}
  /// </summary>
  {{ convertedType this }} {{ name }}{{ getOptionals this }}{ get;{{#unless @root.config.immutableTypes }}set; {{/unless}}  } 
{{/each}}
}
