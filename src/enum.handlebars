/**
<summary>
{{ description }}
</summary>
**/
{{#if @root.config.enumsAsTypes }}
// Code generation does not work here
//[Name("{{ name }}")]
//public enum {{ toPascalCase name }} = {{#each values }}"{{ toPascalCase value }}"{{#unless @last}} | {{/unless}}{{/each}};
{{else}}
[Name("{{ name }}")]
[Description(@"{{ description }}")]
public enum {{ toPascalCase name }} {
{{#each values }}
    [Name("{{ value }}")]
    [Description(@"{{ description }}")]
    @{{ toPascalCase value}}{{#unless @last}}, {{/unless}}
{{/each}}
}
{{/if}}
